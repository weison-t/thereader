# Copy this file to ".env.local" and fill in real values. Do NOT commit ".env.local".

# Public Supabase URL (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Supabase service role key (KEEP SECRET)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Postgres direct URL (KEEP SECRET)
SUPABASE_DB_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# PgBouncer/Pooler URL (KEEP SECRET)
SUPABASE_DB_POOLER_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-1-REGION.pooler.supabase.com:6543/postgres

# 32-byte hex (or similar) application encryption key (KEEP SECRET)
CONFIG_ENCRYPTION_KEY=replace-with-32-byte-hex

# Notes:
# - Set these in Vercel: Project → Settings → Environment Variables.
# - Keep ".env.local" out of git. Rotate any keys that have been shared in logs.
